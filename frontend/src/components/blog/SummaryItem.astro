---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="bg-white px-5 py-5 sm:px-6">
  <p class="text-[0.76rem] font-black uppercase tracking-[0.14em] text-primary">{label}</p>
  <div class="mt-2 text-[1rem] leading-8 text-slate-700">
    <slot />
  </div>
</div>
